name: Dummy

on:
  workflow_call:

jobs:
  dummy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: app
    steps:
      - name: Code Checkout
        uses: actions/checkout@v4
      - run: echo 'Dummy job OK!'
